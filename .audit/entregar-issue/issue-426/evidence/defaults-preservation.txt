The exact-head implementation reuses the pre-existing non-destructive installContractDefaults behavior: supported canonical values are normalized through the Prisma enum, while existing non-empty/customized values are not reinterpreted by the MovementType extension. The material diff introduces no overwrite-all path. Full repository tests passed in Validate PR run 34493868123; the issue-specific movement-type regression passed.
