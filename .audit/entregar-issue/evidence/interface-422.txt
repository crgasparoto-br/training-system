Issue 422 internal interface verification
material_head_sha=fa576c0a154783ec675cdcc10efc20c6e5075382
mode=design-interface/internal-verification
route=/alunos/new and equivalent edit flow

Source-level observations on the frozen material candidate:
- Documentation/social block uses responsive grid classes `grid-cols-1`, `md:grid-cols-3`, and `md:grid-cols-2`; mobile remains single-column.
- CPF and RG inputs use the dedicated formatters during input and edit-load.
- Marital status is a controlled select and conditionally injects the exact unknown legacy value as an option so unrelated saves do not clear it.
- Social network is a controlled select paired with a disabled-until-selected account input and network-specific placeholder.
- Emergency relationship adapter creates a select containing the controlled options, maps an unknown legacy value to `Outra`, preserves the exact custom text, enables a dedicated free-text input only for `Outra`, and dispatches the exact selected/custom value back to the registered form field.
- Visible copy remains user-facing Portuguese; no canonical/legacy storage terminology is exposed in the edited form labels.

Limitation: connector-only runtime did not provide a browser session for this route, so this evidence is source-level internal verification rather than screenshot/E2E proof. Independent audit should exercise representative desktop/mobile interaction if visual runtime evidence is required.
