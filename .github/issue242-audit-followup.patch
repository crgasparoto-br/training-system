diff --git a/apps/api/src/modules/services/service.bootstrap.ts b/apps/api/src/modules/services/service.bootstrap.ts
--- a/apps/api/src/modules/services/service.bootstrap.ts
+++ b/apps/api/src/modules/services/service.bootstrap.ts
@@ -259,7 +259,9 @@ async function executeBootstrap(
   );
 
   const serviceIds = new Map<string, string>();
-  const optionIds = new Map<string, string>();
+  const optionIds = new Map(
+    existing.options.map((row) => [row.code, row.id])
+  );
   const conflictedCodes = new Set<string>();
   const servicesToCreate: ServiceInsert[] = [];
   const optionsToCreate: OptionInsert[] = [];
diff --git a/apps/api/tests/service-catalog-bootstrap.integration.scenarios.ts b/apps/api/tests/service-catalog-bootstrap.integration.scenarios.ts
--- a/apps/api/tests/service-catalog-bootstrap.integration.scenarios.ts
+++ b/apps/api/tests/service-catalog-bootstrap.integration.scenarios.ts
@@ -204,6 +204,45 @@ describeDatabase('service catalog reference bootstrap with PostgreSQL', () => {
     ).resolves.toMatchObject({ name: 'Nome customizado pelo contrato' });
   });
 
+  it('recreates plan components from preserved options when the target service has a name conflict', async () => {
+    await serviceCatalogService.bootstrapReferenceCatalog(contractIds.first, false);
+    const essential = await prisma.serviceOption.findUniqueOrThrow({
+      where: {
+        contractId_code: {
+          contractId: contractIds.first,
+          code: 'plano_essencial',
+        },
+      },
+    });
+    const expectedComponents = await prisma.servicePlanComponent.count({
+      where: { contractId: contractIds.first },
+    });
+
+    await prisma.serviceOption.update({
+      where: { id: essential.id },
+      data: { name: 'Plano Essencial customizado' },
+    });
+    await prisma.servicePlanComponent.deleteMany({
+      where: { contractId: contractIds.first },
+    });
+
+    const result = await serviceCatalogService.bootstrapReferenceCatalog(
+      contractIds.first,
+      false
+    );
+
+    expect(result.conflicts).toEqual(
+      expect.arrayContaining([
+        expect.objectContaining({
+          code: 'plano_essencial',
+          message: expect.stringContaining('Plano Essencial customizado'),
+        }),
+      ])
+    );
+    expect(result.createdComponents).toBe(expectedComponents);
+    await expect(
+      prisma.servicePlanComponent.count({ where: { contractId: contractIds.first } })
+    ).resolves.toBe(expectedComponents);
+  });
+
   it('creates independent records for two contracts', async () => {
     await serviceCatalogService.bootstrapReferenceCatalog(contractIds.first, false);
     await serviceCatalogService.bootstrapReferenceCatalog(contractIds.second, false);
