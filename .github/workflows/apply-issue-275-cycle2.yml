name: Apply Issue 275 Cycle 2

on:
  pull_request:
    branches:
      - develop
    paths:
      - '.github/workflows/apply-issue-275-cycle2.yml'

permissions:
  contents: write

jobs:
  apply-fix:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/275-pre-registration-qa-rollout
          fetch-depth: 0
      - name: Apply keyboard verifier correction
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git apply --index .audit/issue-275-cycle2.patch
          rm .audit/issue-275-cycle2.patch
          git add -A
          git diff --cached --check
          git commit -m 'fix: usar navegação nativa no grupo de perfis'
          git push origin HEAD:feat/275-pre-registration-qa-rollout
