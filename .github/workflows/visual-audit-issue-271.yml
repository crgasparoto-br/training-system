name: Visual Audit Issue 271

on:
  pull_request:
    branches:
      - develop
    paths:
      - 'apps/web/src/pages/PublicPreRegistration/**'
      - 'apps/web/src/services/pre-registration-public.service.ts'
      - 'apps/web/src/App.tsx'
      - 'apps/api/scripts/visual-audit-issue-271.mjs'
      - '.github/workflows/visual-audit-issue-271.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  screenshots:
    name: Audit public pre-registration responsive states
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: feat/271-public-pre-registration
          fetch-depth: 0

      - name: Apply guardian authorization hardening once
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          service_path = Path('apps/api/src/modules/pre-registration-public/pre-registration-public.service.ts')
          service = service_path.read_text()
          if 'normalizeStudentCpf,' not in service:
              service = service.replace(
                  '  normalizeStudentEmail,\n  upsertStudentIdentity,',
                  '  normalizeStudentCpf,\n  normalizeStudentEmail,\n  normalizeStudentPhone,\n  upsertStudentIdentity,',
                  1,
              )
          old = '''  } else {
    await tx.$executeRaw`
      INSERT INTO "PreRegistrationGuardianAuthorization" (
        "id", "contractId", "alunoId", "guardianUserId", "purpose", "status",
        "validatedAt", "validatedByUserId", "createdAt", "updatedAt"
      ) VALUES (
        ${crypto.randomUUID()}, ${invite.contractId}, ${invite.alunoId}, ${userId},
        'PRE_REGISTRATION', 'ACTIVE', CURRENT_TIMESTAMP, ${userId},
        CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
      )
      ON CONFLICT ("alunoId", "guardianUserId", "purpose")
      DO UPDATE SET
        "status" = 'ACTIVE',
        "validatedAt" = CURRENT_TIMESTAMP,
        "validatedByUserId" = ${userId},
        "revokedAt" = NULL,
        "revokedByUserId" = NULL,
        "updatedAt" = CURRENT_TIMESTAMP
    `;
    await startGuardianPreRegistrationInTransaction(
'''
          new = '''  } else {
    const identity = await loadStudentIdentity(invite.alunoId, invite.contractId, tx);
    const guardianEmailMatches = Boolean(
      identity.guardianEmail &&
      normalizeStudentEmail(identity.guardianEmail) === normalizeStudentEmail(user.email)
    );
    const guardianCpfMatches = Boolean(
      identity.guardianCpf &&
      user.profile?.cpf &&
      normalizeStudentCpf(identity.guardianCpf) === normalizeStudentCpf(user.profile.cpf)
    );
    const guardianPhoneMatches = Boolean(
      identity.guardianPhone &&
      user.profile?.phone &&
      normalizeStudentPhone(identity.guardianPhone) === normalizeStudentPhone(user.profile.phone)
    );
    if (!guardianEmailMatches && !guardianCpfMatches && !guardianPhoneMatches) {
      throw new PreRegistrationPublicError(
        'Não foi possível validar seu vínculo com o menor. Solicite a revisão da academia.',
        'GUARDIAN_AUTHORIZATION_REQUIRED'
      );
    }

    await tx.$executeRaw`
      INSERT INTO "PreRegistrationGuardianAuthorization" (
        "id", "contractId", "alunoId", "guardianUserId", "purpose", "status",
        "validatedAt", "validatedByUserId", "createdAt", "updatedAt"
      ) VALUES (
        ${crypto.randomUUID()}, ${invite.contractId}, ${invite.alunoId}, ${userId},
        'PRE_REGISTRATION', 'ACTIVE', CURRENT_TIMESTAMP, ${userId},
        CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
      )
      ON CONFLICT ("alunoId", "guardianUserId", "purpose")
      DO UPDATE SET
        "status" = 'ACTIVE',
        "validatedAt" = CURRENT_TIMESTAMP,
        "validatedByUserId" = ${userId},
        "revokedAt" = NULL,
        "revokedByUserId" = NULL,
        "updatedAt" = CURRENT_TIMESTAMP
    `;
    await startGuardianPreRegistrationInTransaction(
'''
          if old in service:
              service = service.replace(old, new, 1)
              service_path.write_text(service)

          test_path = Path('apps/api/tests/pre-registration-public.integration.test.ts')
          tests = test_path.read_text()
          first_anchor = '''    firstAlunoId = first.id;

    const second = await prisma.aluno.create({'''
          if first_anchor in tests:
              tests = tests.replace(first_anchor, '''    firstAlunoId = first.id;
    await upsertStudentIdentity(
      first.id,
      contractId,
      { guardianEmail: `${suffix}-guardian@example.com` },
      {
        actor: { userId: guardianUserId },
        sourceType: 'professional',
        sourceReference: 'issue_271_guardian_validation_fixture',
      }
    );

    const second = await prisma.aluno.create({''', 1)
          second_anchor = '''    secondAlunoId = second.id;
  });'''
          if second_anchor in tests:
              tests = tests.replace(second_anchor, '''    secondAlunoId = second.id;
    await upsertStudentIdentity(
      second.id,
      contractId,
      { guardianEmail: `${suffix}-guardian@example.com` },
      {
        actor: { userId: guardianUserId },
        sourceType: 'professional',
        sourceReference: 'issue_271_guardian_validation_fixture',
      }
    );
  });''', 1)
          test_path.write_text(tests)

          workflow = Path('.github/workflows/visual-audit-issue-271.yml')
          workflow.write_text('''name: Visual Audit Issue 271

on:
  pull_request:
    branches:
      - develop
    paths:
      - 'apps/web/src/pages/PublicPreRegistration/**'
      - 'apps/web/src/services/pre-registration-public.service.ts'
      - 'apps/web/src/App.tsx'
      - 'apps/api/scripts/visual-audit-issue-271.mjs'
      - '.github/workflows/visual-audit-issue-271.yml'
  workflow_dispatch:

jobs:
  screenshots:
    name: Audit public pre-registration responsive states
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chrome for Puppeteer
        run: pnpm --filter @corrida/api exec puppeteer browsers install chrome

      - name: Build web application
        run: |
          pnpm --filter @corrida/types build
          pnpm --filter @corrida/utils build
          pnpm --filter @corrida/web build

      - name: Audit routes, states and responsive layouts
        shell: bash
        run: |
          set -o pipefail
          pnpm --filter @corrida/api exec node scripts/visual-audit-issue-271.mjs 2>&1 | tee visual-audit.log

      - name: Upload screenshots and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: issue-271-visual-${{ github.run_id }}
          path: |
            artifacts/issue-271-visual/*.png
            visual-audit.log
          if-no-files-found: warn
          retention-days: 14
''')
          PY

          if git diff --quiet; then
            exit 0
          fi
          git add -A
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'fix: validar vínculo do responsável na issue 271'
          git push origin HEAD:feat/271-public-pre-registration

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chrome for Puppeteer
        run: pnpm --filter @corrida/api exec puppeteer browsers install chrome

      - name: Build web application
        run: |
          pnpm --filter @corrida/types build
          pnpm --filter @corrida/utils build
          pnpm --filter @corrida/web build

      - name: Audit routes, states and responsive layouts
        shell: bash
        run: |
          set -o pipefail
          pnpm --filter @corrida/api exec node scripts/visual-audit-issue-271.mjs 2>&1 | tee visual-audit.log

      - name: Upload screenshots and diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: issue-271-visual-${{ github.run_id }}
          path: |
            artifacts/issue-271-visual/*.png
            visual-audit.log
          if-no-files-found: warn
          retention-days: 14
