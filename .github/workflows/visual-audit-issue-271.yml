name: Issue 271 package staging

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary staging workflow; replaced by the orchestrated package."
