name: Validate PR

on:
  pull_request:
    branches:
      - main
      - develop
  push:
    branches:
      - main
      - develop
      - 'chore/**'
      - 'feat/**'
      - 'fix/**'

jobs:
  issue_246_bootstrap:
    name: Stage issue 246 bundle
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Bundle parts are being staged.'
